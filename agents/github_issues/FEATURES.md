# Features — Dev Issues

| Issue | Summary | Motivation | Status | Notes |
|-------|---------|------------|--------|-------|
| #1 | Define MCIP v1 (10 record types, hashing/versioning rules, conformance fixtures) | Shared interchange contract with `llm-driven-proof-search`, independent of its SQLite schema | done | `schema/mcip/v1/`, `tools/validate_mcip.py`, `tools/gen_mcip_fixtures.py`. Foundation for #2-#7. |
| #2 | Proof profiles, restriction profiles, proof variants on packets | Packets can't distinguish proof strategy or represent multiple proof styles | done | `packet.schema.json` `proof_variants[]`, shared `restriction_profiles/` catalog, `policy.check_proof_variants`/`check_restriction_profile_refs`, 5 elementary algebra packets migrated. |
| #3 | Standardize dependency manifests and retrieval evidence | `dependencies` is too shallow; `author_packet.py` doesn't populate real usage | done | `packet.schema.json` `dependency_manifest`, corpus-wide dangling/cycle checks, `dependency_graph.json` aggregation, `author_packet.py` fixed + extended. |
| #4 | Negative-example and repair-trajectory child records | Only a minimal `failure` summary today; no repair chains | done | `packet.schema.json` `attempts[]`/`negative_examples[]`/`repair_trajectories[]`, hash-linked repair steps, corpus-wide terminal_ref check, real gcd_dvd repair example + synthetic diagnostics-conformance fixture. |
| #5 | Multi-model empirical difficulty + native analytics exports | `difficulty_bin` is author-assigned only; enrichment hidden in nested JSON | done | `packet.schema.json` `model_runs[]`/`empirical_difficulty_aggregates[]`, reproducible calibration formula, `tools/mathcorpus/aggregates.py` corpus reporting, native Parquet columns. |
| #6 | MCIP importer, packet enrichment workflow, 190-packet backfill | Make enriched Proof Search output the default population path | done | `tools/import_mcip.py` + `tools/mathcorpus/mcip_import.py` (idempotent, conflict-quarantining), `author_packet.py` `verifier_export_bundle`, `tools/backfill_report.py`. Backfill is honestly partial — no retained Proof Search trajectory DB in this environment, see `docs/mcip-import.md`. |
| #7 | Literature-lineage, idea-attribution, prior-art records | Packet-level provenance isn't enough for AI-assisted proofs | open | New MCIP-compatible child schemas (`LiteratureSource`, `RetrievedPassage`, etc.) not yet defined — out of #1's original 10 record types. |
| #8 | Contribution/citation/novelty review policy for open-problem exports | Kernel-verified isn't the same as publication-ready | open | Policy-only; depends on #7's attribution records existing first. |
