# Done — Dev Issues

Completed issues, most recent first.

| Date | Issue | Summary | Fix commit |
|------|-------|---------|------------|
| 2026-07-12 | #7 | Literature-lineage, idea-attribution, prior-art records: 7 new MCIP v1 schemas (`LiteratureSource`, `RetrievedPassage`, `ExternalClaim`, `IdeaAttribution`, `PriorArtMatch`, `CitationReview`, `ContributionStatement` — schema/mcip/v1/) plus matching packet-embedded shapes (`idea_attributions[]`, `prior_art_matches[]`, `citation_reviews[]`, `contribution_statements[]`). `LiteratureSource`/`RetrievedPassage`/`ExternalClaim` live in a new shared, hash-pinned `literature_sources/` catalog (same pattern as `restriction_profiles/` from #2), with `tools/stamp_literature_sources.py`. `attribution_status` + `visibility` (`model_visible`/`post_hoc`) on `IdeaAttribution` and the structural separation of `PriorArtMatch` (inherently post-hoc, no shared field needed) is the mechanism that keeps model-visible and post-hoc evidence from being conflated. `source_sha256_pin` hash-pins every attribution to its catalog entry. `contribution_class` vocabulary (`new_proof`/`independent_rediscovery`/`formalization`/`verification`/`reconstruction`/`adaptation`/`literature_derived_synthesis`) defined here for #8 to require via policy. New `policy.check_literature_lineage` (per-packet dangling refs, statement-hash consistency), `check_literature_source_refs` + `check_literature_source_catalog` (corpus-wide catalog resolution, redaction-contradiction check). **Real fixture, not fabricated**: the acceptance criterion's "CDC"/"Fano-flow" reference nothing that exists in this repo (confirmed by search) — substituted `packets/frontier/formal_conjectures/union_closed_sharpness.v1.json`, a real packet whose own pre-existing notes already document its FormalConjectures source, Wikipedia background, and a distinct prior-art bound (Yu 2023's improved general-case constant), enriched with real `contribution_class: "adaptation"` (it's a transported/adapted proof, not a `new_proof`) identified separately from the Yu 2023 prior art. Yu 2023's bibliographic precision is explicitly marked unverified rather than invented. **Cross-cutting fix found via testing**: `schema/mcip/v1/_defs.schema.json` had an invalid `envelope_required` key (a plain array masquerading as a subschema under `$defs`, failing strict `check_schema` — a latent bug from #1, never triggered before because nothing previously ran `check_schema` across every file in the registry including `_defs.schema.json` itself) — removed (it was unused documentation with zero references anywhere). 10 failure modes smoke-tested directly. Full corpus (357 packets) + full CI-equivalent pipeline + issue #6 tooling (importer dry-run, backfill report) verified locally, 0 errors. | (local, unpushed — see HANDOFF.md) |
| 2026-07-12 | #6 | MCIP importer, packet enrichment workflow, backfill report: new `tools/import_mcip.py` folds an MCIP bundle's records into an existing packet's child records via shared `tools/mathcorpus/mcip_import.py` (`fold_proof_variant`, `fold_dependency_manifest`, etc., with MCIP→packet field-mapping: renames, envelope-stripping, `kits`/`tactic_tags`/`prerequisite_concepts` moved onto the shallow `dependencies` object). Structurally-invalid bundles rejected outright; a bundle whose target packet is missing or whose `formal_statement_sha256` disagrees is quarantined whole; within a valid bundle, individual conflicting records are quarantined while the rest still applies; re-running the same bundle is a verified no-op (byte-identical output), making batches resumable. Canonical packet identity fields verified unchanged after import. `restriction_profile` records go to the shared catalog via a new `import_restriction_profile`, same conflict/idempotency semantics. `author_packet.py` gains an optional `verifier_export_bundle` spec field using the same fold logic — manual spec fields win on conflict (reported, never silently overwritten); fixed a real 2-vs-3-tuple unpacking bug caught by testing this path. New `tools/backfill_report.py` gives every packet a per-field (7 recoverable-target categories) status — `recovered` / `not_yet_backfilled` / `unrecoverable_missing` / `not_applicable` — honestly marking `attempts_and_diagnostics` and `model_hashes_tokens_cost_timing` as unrecoverable for ~355 of 357 packets since this environment has no retained Proof Search trajectory database (documented explicitly in `docs/mcip-import.md`, not silently glossed over). New optional `validate_packets.py --artifact-index` + `policy.check_trajectory_artifact_resolution` implements "provenance resolves to trajectory records when policy allows" as a policy-gated, off-by-default mechanism (no real artifact store exists yet to require it against). CI gains an importer dry-run + backfill-report step. **Cross-cutting fixes found via this integration work**: `schema/mcip/v1/empirical_difficulty_aggregate.schema.json` was missing `calibration_version` (present on the packet-embedded version since #5) — added, fixtures regenerated using the real `difficulty.compute()` formula instead of hand-picked values; `packet.schema.json`'s `proof_variant` was missing `model_run_id` (present in MCIP's own version since #1) — added. A misleading importer summary message (claiming packet hashes were recomputed when only a restriction-profile catalog file was written) was caught and fixed via testing. Extensive smoke-testing throughout (dry-run, apply+validate, idempotency, canonical-identity preservation, all 3 bundle-level quarantine paths, restriction-profile conflict, author_packet.py conflict path). Full corpus (357 packets) + full CI-equivalent pipeline verified locally, 0 errors. | (local, unpushed — see HANDOFF.md) |
| 2026-07-12 | #5 | Multi-model empirical difficulty + native analytics exports: `packet.schema.json` gains `model_runs[]`/`empirical_difficulty_aggregates[]` ($defs `model_run`/`empirical_difficulty_aggregate`), mirroring MCIP's shapes from #1, strictly additive to the author-assigned top-level `difficulty_bin` (nothing writes to it from this data). New `tools/mathcorpus/difficulty.py` (versioned `calibration_version: "v1"` formula: `1 -` mean `eventual_pass_rate` → fixed-threshold bin); `policy.check_empirical_difficulty_aggregates` recomputes from the packet's own `model_runs` and rejects a mismatch, so aggregates are reproducible rather than asserted, plus a dangling-model-run-id check, a stale-author-bin warning, and a duplicate-aggregate-signature warning. `model_run.censored_fields[]` distinguishes "deliberately withheld" from `null`="not collected". New shared `tools/mathcorpus/aggregates.py` (proof-class distribution, dependency depth, negative counts, per-model-family pass rate) used by both `build_manifests.py` (refactored, no logic duplication) and an extended `corpus_stats.py`. `tools/export_parquet.py` hoists 9 enrichment summaries into native Parquet columns instead of leaving them in the `packet_json` string blob; `_flatten()` logic verified directly since pyarrow isn't installed in this environment. No real multi-model evaluation data exists yet (Phase 7 of the roadmap hasn't started), so a new explicitly-synthetic `packets/_fixtures/model_performance_conformance.v1.json` fixture (private_audit_only, excluded from every export lane) demonstrates the schema with a genuinely recomputed (not hand-picked) aggregate. 9 failure modes smoke-tested directly. Full corpus (357 packets) + full CI-equivalent pipeline verified locally, 0 errors. | (local, unpushed — see HANDOFF.md) |
| 2026-07-12 | #4 | Negative-example / repair-trajectory child records: `packet.schema.json` gains `attempts[]`, `negative_examples[]`, `repair_trajectories[]` ($defs `diagnostic`/`attempt_record`/`negative_example_record`/`repair_trajectory`), mirroring MCIP's shapes from #1, additive to the existing packet-level `failure` object. `negative_example_record.proof_authority` fixed to `"none"` at the schema level. `repair_trajectory` steps are hash-linked via a new `hashing.repair_step_hash` (real SHA-256 over step content, not a placeholder). `policy.check_attempts_and_repairs` catches dangling attempt/variant refs, unordered steps, stale step hashes, and negative-example/attempt diagnostic-category disagreement (warning); new corpus-wide `policy.check_repair_trajectory_refs` catches a dangling cross-packet `terminal_ref`. **Real enrichment, not fabricated**: `packets/negative/number_theory/gcd_dvd_omega_no_gcd_theory.v1.json` gets a true `unsolved_goals` attempt + a true successful repair, its `terminal_ref` pointing at the actual companion packet (`elementary.number_theory.gcd_dvd_left.v1`) where that proof already lives — reconstructed from the packet's own pre-existing notes, not invented. The 3 diagnostic categories with no real corpus precedent (`parse_failure`, `unknown_declaration`, `type_mismatch`) are covered by a new, explicitly-labeled `packets/negative/_fixtures/diagnostic_categories_conformance.v1.json` fixture — `kind: concept` + `training.eligibility: private_audit_only` so it's excluded from every export lane, confirmed by grep after a full export run. 8 failure modes smoke-tested directly. Full corpus (356 packets) + full CI-equivalent pipeline verified locally, 0 errors. | (local, unpushed — see HANDOFF.md) |
| 2026-07-11 | #3 | Dependency manifests: `packet.schema.json` gains `dependency_manifest` ($defs `dependency_manifest`/`dependency_claim_source`) distinguishing declared/used/obligation/verified-module-item/retrieval-candidate/retrieved-unused categories, additive to the shallow `dependencies` object. `policy.check_dependency_manifest` catches environment-hash mismatch, used/retrieved-unused contradictions, and claim_sources entries not present in their named category array; new corpus-wide `policy.check_dependency_manifest_refs` catches dangling MathCorpus packet_id references and dependency cycles (rotation-deduped via edge-set signature). `tools/build_manifests.py`'s `dependency_graph.json` gains a `dependency_manifest_summary` (frequency per dependency id per category, min/max/avg transitive depth). Fixed `author_packet.py` silently dropping `theorem_deps` from spec input (issue's own complaint); added optional `dependency_manifest` spec section. 5 elementary algebra packets migrated with real (non-fabricated) dependency evidence read from their actual Lean proof bodies. 6 failure modes smoke-tested directly (env mismatch, used/unused contradiction, unlisted claim, dangling ref, 2-cycle, cycle-dedup verified to report once not twice). Full CI-equivalent pipeline verified locally, 0 errors. The acceptance criterion "kernel-verified Proof-Search packets must reference a manifest" is intentionally *not* a hard gate yet — enforcing it now would fail all ~350 pre-existing packets; deferred to the #6 backfill, documented in `schema/ENUMS.md`. | (local, unpushed — see HANDOFF.md) |
| 2026-07-11 | #2 | Proof profiles, restriction profiles, proof variants: `packet.schema.json` gains `proof_variants[]` ($defs `proof_variant`/`proof_profile`, mirroring MCIP's shapes) — canonical/shortest/pedagogical/restricted/interactive/alternate_model styles, `restricted` requires a hash-pinned restriction profile. New shared `restriction_profiles/` catalog (reuses `schema/mcip/v1/restriction_profile.schema.json` directly — no duplicate schema) + `tools/stamp_restriction_profiles.py`. `tools/mathcorpus/policy.py` gains `check_proof_variants` (statement/environment/canonical-proof hash must match parent packet) and `check_restriction_profile_refs` (hash-pin must resolve to a current catalog entry); `tools/validate_packets.py` now also validates the catalog automatically (no CI change needed — same `packets/` invocation). Registry-building helpers factored out of `validate_mcip.py` into `tools/mathcorpus/mcip.py` so both tools share one $ref-resolving jsonschema registry. 5 elementary algebra packets (add_assoc, add_comm, add_left_comm, add_mul, add_zero) migrated as fixtures — all with a `canonical` variant, 2 (add_assoc, add_mul) additionally with a `restricted` variant hash-pinned to a shared `no_ring_automation` profile (verified true of their actual Lean proof bodies, not fabricated). 4 new failure modes smoke-tested directly (variant statement/proof hash mismatch, dangling restriction-profile ref, stale hash pin). Full CI-equivalent pipeline (manifests/split/dedupe/export/redaction-audit) run locally end-to-end, 0 errors. | (local, unpushed — see HANDOFF.md) |
| 2026-07-11 | #1 | MCIP v1: 10 record schemas + bundle envelope under `schema/mcip/v1/`, canonicalization/hashing/version-negotiation rules documented in `schema/mcip/v1/README.md`, `tools/validate_mcip.py` (standalone bundle validator, no Proof Search dependency), `tools/gen_mcip_fixtures.py` + 4 conformance fixture bundles (success proof, failed attempt, repair chain, multi-model aggregate — 17 records, all validate clean with `--check-hashes`), CI step added. Fail-closed behavior (unknown fields, tampered hashes, `NegativeExample.proof_authority` const) smoke-tested directly against the validator. | 7f1ac10 (local, unpushed — see HANDOFF.md) |
