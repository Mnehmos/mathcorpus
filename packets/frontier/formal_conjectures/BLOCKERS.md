# Blockers — Formal Conjectures

| Date | Packet | Blocker | Route Attempted | Status |
|------|--------|---------|------------------|--------|
| 2026-07-08 | `Mathoverflow/75792.lean::complexity_five_pow` (integer complexity of `5^n` != `5n`, disproved at `n=6` via `5^6 = 1 + 2^3*3^2*(1 + 2^3*3^3)` needing only 29 ones) | The theorem statement and its proof genuinely have NO `sorry` in the sibling `formal-conjectures` checkout (a rare, real "just transport it" candidate) — but it and every lemma it depends on (`Reachable.five_pow_six`, `Reachable.complexity_le`, `complexity`) are built on a custom `inductive Reachable : ℕ → ℕ → Prop` type (constructors `one`/`add`/`mul`). `SubmitModule`'s `LeanModuleItem` only supports item kinds `def`, `theorem`, `mutual_group` — there is no `inductive`/`structure` item kind, so a custom inductive type cannot be transported via the structured-items mechanism at all (same category of hard blocker as `#1113`'s `set_option`, not a transcription issue). | Read the full dependency chain (`Reachable` + 5 lemmas), confirmed no `sorry`, then confirmed the `SubmitModule` schema has no inductive-type item kind. Not attempted via `episode_step` (no viable action shape exists). | **General, reusable finding — check this BEFORE spending time reading a candidate's proof body**: if a "research solved, no sorry" candidate's statement or proof depends on a custom `inductive` (not just `def`) declared in the same file, it is not transportable via this environment's `SubmitModule` at all, regardless of proof-body complexity. A workaround (Church-encoding the inductive type as an impredicative `def`, e.g. `def Reachable (m n : ℕ) := ∀ P, (closure conditions) → P m n`) is possible in principle but requires re-deriving every dependent proof in the new impredicative style (the original inductive-pattern-match proofs don't transfer) — that crosses into "original formalization," not transport, and was not attempted here. |
