# Blockers — Formal Conjectures

| Date | Packet | Blocker | Route Attempted | Status |
|------|--------|---------|------------------|--------|
| 2026-07-08 | `Mathoverflow/75792.lean::complexity_five_pow` (integer complexity of `5^n` != `5n`, disproved at `n=6` via `5^6 = 1 + 2^3*3^2*(1 + 2^3*3^3)` needing only 29 ones) | The theorem statement and its proof genuinely have NO `sorry` in the sibling `formal-conjectures` checkout (a rare, real "just transport it" candidate) — but it and every lemma it depends on (`Reachable.five_pow_six`, `Reachable.complexity_le`, `complexity`) are built on a custom `inductive Reachable : ℕ → ℕ → Prop` type (constructors `one`/`add`/`mul`). `SubmitModule`'s `LeanModuleItem` only supports item kinds `def`, `theorem`, `mutual_group` — there is no `inductive`/`structure` item kind, so a custom inductive type cannot be transported via the structured-items mechanism at all (same category of hard blocker as `#1113`'s `set_option`, not a transcription issue). | Read the full dependency chain (`Reachable` + 5 lemmas), confirmed no `sorry`, then confirmed the `SubmitModule` schema has no inductive-type item kind. Not attempted via `episode_step` (no viable action shape exists). | **General, reusable finding — check this BEFORE spending time reading a candidate's proof body**: if a "research solved, no sorry" candidate's statement or proof depends on a custom `inductive` (not just `def`) declared in the same file, it is not transportable via this environment's `SubmitModule` at all, regardless of proof-body complexity. A workaround (Church-encoding the inductive type as an impredicative `def`, e.g. `def Reachable (m n : ℕ) := ∀ P, (closure conditions) → P m n`) is possible in principle but requires re-deriving every dependent proof in the new impredicative style (the original inductive-pattern-match proofs don't transfer) — that crosses into "original formalization," not transport, and was not attempted here. **UPDATE 2026-07-08 (round 3, same day)**: this blocker is not always fatal — see the follow-up row below for a case where the same class of blocker had a genuine two-line workaround. The distinguishing question: is the `inductive`/`structure`/`class` *load-bearing* (the proof pattern-matches on its constructors, as `Reachable`'s does) or *purely notational* (a thin wrapper around a function/data type the proof never actually inspects as a class, as below)? Only the load-bearing case is a hard stop. |
| 2026-07-08 | `Other/EquationalTheories_677_255.lean::Equation255_not_implies_Equation677` (+ `Finite.` variant) — Equational Theories project (Tao et al.), `Fin 3` magma counterexample | Same surface blocker as `complexity_five_pow` above: the witnesses are `Magma G` instances, and `class` is exactly the same "custom inductive-shaped item" `SubmitModule` can't accept. | Read `Magma`/`Equation255`/`Equation677`'s definitions: `class Magma (α) where op : α → α → α`, and `Equation255 G := ∀ x, x = ((x◇x)◇x)◇x` (a plain `abbrev` over `Magma.op`, i.e. notation, not structure). The actual proof (`fin_cases x <;> rfl`, `of_decide_eq_false rfl`) never pattern-matches on `Magma` as a class — it only ever uses `Magma.op` as a plain function. **Not blocked**: restates cleanly as `∃ op : Fin 3 → Fin 3 → Fin 3, (∀ x, x = op (op (op x x) x) x) ∧ ¬∀ x y, x = op y (op x (op (op y x) y))`, zero custom types, `op i j := i+j+1` (verified against the upstream table by hand). See `SOLVED_QUEUE.md` round-3 for the full sketch. | **Resolved as a non-blocker, with a general lesson**: `class`/`structure` blocks `SubmitModule` in principle, but if every proof step treats the class as "just a function/tuple" rather than pattern-matching its constructor, the class can be erased in the restatement with zero mathematical loss — turn the instance into a bare argument of its underlying type. **Packetized 2026-07-08** as `frontier.formal_conjectures.equational_theories_677_255_class_free.v1` (episode `39013082-f757-41f2-b26b-420d7577a454`). |
| 2026-07-08 | `OEIS/34693.lean::exists_k_best_possible` (A34693: the bound `1 + n^0.74` is not always valid, witness `n=19`) | Upstream proof term uses `Real.nthRoot 100 n ^ 74`, but `Real.nthRoot` fails to resolve in this repo's pinned Mathlib revision (`kernel_fail`: "Unknown constant `Real.nthRoot`"). `mathlib_search_declarations` confirms this environment's own `nthRoot` is a DIFFERENT, Nat-valued function (`Mathlib/Analysis/SpecialFunctions/Pow/NthRootLemmas.lean`, signature `nthRoot : ℕ → ℕ → ℕ`) — a genuine cross-Mathlib-revision API drift between the sibling `formal-conjectures` checkout and this repo's pinned toolchain, not a transcription or proof error. | Verified via `lean_declaration_lookup`/`mathlib_search_declarations` that `Real.nthRoot` is absent and `nthRoot` means something else here. Restated the root statement using `Real.rpow` directly (`(n:ℝ) ^ ((74:ℝ)/100)` instead of `Real.nthRoot 100 n ^ 74` — mathematically identical for `n ≥ 0`), then re-derived the same numeric bound via `Real.rpow_natCast`/`Real.rpow_mul`/`pow_le_pow_iff_left₀` instead of the upstream `nthRoot`-unfolding lemmas. **Not blocked** — `Real.rpow` is standard Mathlib and always available; reached kernel_verified on the second attempt (episode `406c3860-6fcf-40b3-82a5-4a3a1726a89f`; first attempt also hit an unrelated `raw_lean_block` indentation bug — see that packet's `notes` field). Packetized as `frontier.formal_conjectures.oeis_a34693_exists_k_best_possible.v1`. | **General, reusable finding**: an upstream `formal-conjectures` file being genuinely `sorry`-free does NOT guarantee its exact proof term transports verbatim — the sibling checkout may be pinned to a different Mathlib revision than this repo, so an API name it uses (especially less-common ones like `Real.nthRoot`, `Real.nthRoot` vs a same-named-but-different-signature `nthRoot`) can be absent, renamed, or shadowed here. Before assuming a verbatim transport will compile, spot-check any unusual API names via `mathlib_search_declarations`/`lean_declaration_lookup` (with `deep_check: true` if the fast path fails) — an `unknown identifier`/`unknown constant` kernel_fail on an otherwise-correct proof is often this class of drift, fixable by re-deriving the same fact through an available equivalent API, not a sign the underlying math or proof strategy is wrong. |
