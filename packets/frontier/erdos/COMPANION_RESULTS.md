# Companion Results — Erdős Frontier

Known, already-solved companion theorems that support an Erdős target
(e.g. bounds, special cases, or classical results the open problem builds
on). Safe to packetize once Lean-verified — these do **not** count as
solving the open problem itself. Rows below are already Lean-verified in
the sibling repo (`F:\Github\mnehmos.llm-driven-proof-search.environment\ErdosProblems\`,
2026-07-07); "Packet" is empty until authored here.

| Result | Supports problem | Lean status | Source repo path | Packet |
|--------|--------------------|-------------|---------------------|--------|
| `integer_isGoodPair_iff` cluster (7 thms) | #349 | kernel_verified | `ErdosProblems/erdos-349/proof/` | **CLUSTER COMPLETE — 6/7 packetized** (the 7th is a corpus-shape `True ↔ ...` wrapper not worth a separate packet): `frontier.erdos.erdos_349_exists_finset_sum_two_pow.v1`, `frontier.erdos.erdos_349_one_two_is_good_pair.v1` (`(1,2)` is good), `frontier.erdos.erdos_349_alpha_le_one_not_is_good_pair.v1` (`α ≤ 1` fails), `frontier.erdos.erdos_349_alpha_gt_two_not_is_good_pair.v1` (`α > 2` fails, 118 LOC), `frontier.erdos.erdos_349_int_coeff_ge_two_not_is_good_pair.v1` (integer `t ≥ 2` fails), and the culminating **`frontier.erdos.erdos_349_integer_is_good_pair_iff.v1`** (episode `5edbb136-...`, the full theorem: for positive integers `t, α`, `(t,α)` is good iff `t=1 ∧ α=2` — 280 LOC/~210 tactics, by far the largest proof transported in this lane, all four sublemmas re-embedded as local `have`s exactly mirroring the sibling repo's isolated-`problem_version` structure, kernel_verified on the FIRST attempt). All single `Solve` steps; `training.eligibility: quarantined`. **De-risking lesson**: debugging each sublemma as its OWN standalone packet first, then re-embedding the already-working tactic scripts verbatim into the big combined proof, made the 280-line culminating assembly a first-try success despite its size — build up complex multi-lemma targets incrementally like this rather than attempting the full combination cold. **Timeout lesson**: a closely related file, `Erdos349_dyadic_two_isGoodPair.lean` (the `(1/2^k, 2)` fiber, needs `field_simp`/`push_cast` over a division), timed out twice at the environment's ~60s wall-clock cap on an otherwise-identical (but much smaller) proof shape — see `BLOCKERS.md`; length/complexity isn't a reliable timeout predictor either way (the 280-line proof succeeded fine), so just try it. **Hash-match note**: two of the six packets (`int_coeff_ge_two`, `integer_is_good_pair_iff`) had `root_statement_hash` NOT matching their source comments' cited hashes despite careful transcription (recorded as `adapted_with_review`); the other four matched exactly (`canonical_statement_hash_match`) — don't treat a mismatch as reason to distrust a transcription, it's likely just a whitespace/encoding difference between environments. |
| Subbarao–Warren: unitary perfect numbers are even | #1052 | kernel_verified | `ErdosProblems/erdos-1052/proof/` | (none yet) |
| Steinerberger: `gcd(aₙ,Lₙ)>1` infinitely often | #291 (part ii) | kernel_verified | `ErdosProblems/erdos-291/proof/` | `frontier.erdos.erdos_291_infinite_gcd_gt_one.v1` (2026-07-08, episode `2f8943e6-55c8-4e3b-88c0-9c848b14482e`, `SubmitModule` with 2 defs + 5 helper theorems + root, re-verified independently in this repo's own pinned toolchain; `training.eligibility: quarantined`, same policy as the other rows). **Important SubmitModule lesson** (took 4 attempts, the largest module transported here so far): helper items are ALWAYS forced through the flattener regardless of requested `proof_format`, so (1) any `have h := by tac1; tac2` followed by more sibling tactics MUST be parenthesized as `have h := (by tac1; tac2)` or the flattener silently swallows the trailing tactics into the inner `by` block (issue #67's flattened-sequence trap — this bit FOUR separate `have`s across two different helpers before it was fully caught), and (2) `·`-bullet case splits inside a helper do not survive flattening at all and must be rewritten bullet-free (e.g. via `Or.elim`/`.elim` term-mode dispatch) — only the root theorem can opt into `proof_format: raw_lean_block` to preserve bullets. Audit every helper item's `have`s for this before submitting a multi-item module. |
| Cambie: no `n! = x⁴+y⁴` with `gcd(x,y)=1`, `xy>1` | #399 | kernel_verified | `ErdosProblems/erdos-399/proof/` | `frontier.erdos.erdos_399_cambie.v1` (2026-07-08, episode `d2aaae74-a8c1-406a-a196-ce132e64307e`, re-verified independently in this repo's own pinned toolchain; `training.eligibility: quarantined` per default-quarantine policy for `open_problem_related` packets pending external review) |
| Steinerberger counterexample: product version false | #494 | kernel_verified | `ErdosProblems/erdos-494/proof/` | `frontier.erdos.erdos_494_product.v1` (2026-07-08, episode `d4688926-661b-424e-b135-18a98da1bc73`, `SubmitModule` with 1 def + 2 theorems, re-verified independently in this repo's own pinned toolchain; `training.eligibility: quarantined`, same policy as the #399/#349 rows). Note for future SubmitModule work: the sibling repo's proof relies on `open Finset` for the unqualified `mapEmbedding` name — module_items have no room for an `open` preamble, so fully-qualify as `Finset.mapEmbedding` (or the equivalent for any other `open`-dependent name) when transporting proofs this way. |
| Sierpiński (1960): infinitely many Sierpiński numbers | #1113 | kernel_verified, no native_decide | `ErdosProblems/erdos-1113/proof/` | (none yet) |
| `σ*` on prime powers/primes (not in Mathlib before this work) | #1052 (infrastructure) | kernel_verified | `ErdosProblems/erdos-1052/proof/Erdos1052_sigmaStar_and_bounds.lean::sigmaStar_prime_pow`, `::sigmaStar_prime` | `frontier.erdos.erdos_1052_sigmastar_prime_pow_and_prime.v1` (2026-07-08, episode `0eb1d5e8-4af9-4980-930c-52979b3e5cc9`, `SubmitModule` with 2 defs `uDiv`/`sigmaStar` + 2 theorems `mem_uDiv`/`sigmaStar_prime_pow` + root `sigmaStar_prime`, kernel_verified on the FIRST attempt after de-bulleting `sigmaStar_prime_pow`'s nested `.`-branch case split; re-verified independently in this repo's own pinned toolchain; `training.eligibility: quarantined`). **`sigmaStar_mul_of_coprime` (the general multiplicativity theorem) is deliberately NOT included** — its proof is a 5-branch `Finset.sum_nbij'` with nested bullet case splits, a substantially bigger bullet-free rewrite scoped out of this cycle on purpose (see the lane's incremental-transport practice). **Still open / priority target**: `sigmaStar_mul_of_coprime` itself, and the `omega_odd_le_two_adic_add_one` structural bound that depends on it — pack the multiplicativity theorem next so the bound can cite it rather than re-deriving inline. |

Also worth tracking from `docs/erdos/bounty-board.md`'s Lane 1 ("solved-with-`sorry` inside bounty files, immediate wins", not yet attempted in the sibling repo):

- `#470 erdos_470.variants.weird_pos_density` — weird numbers have positive
  density (Benkoski–Erdős 1974); ships as `sorry` in the corpus, no attempt
  made yet. **Correction (this agent, source-reviewed 2026-07-08, see
  `SOURCE_REVIEW.md`): this is NOT actually a Lane-1-style "immediate
  win."** Every other Lane 1 item this session (#1052's evenness proof,
  #291.ii, #349, #399, #494) was a `sorry` for a result someone had
  *already proved in Lean somewhere* — pure transport/re-verification
  work. `weird_pos_density` has no existing Lean proof anywhere to
  transport; it would need an original formalization of a genuine
  analytic-number-theory density argument (Benkoski–Erdős's actual 1974
  proof), built on `Set.HasPosDensity` (asymptotic `liminf` of
  `Set.partialDensity`, defined in
  `FormalConjecturesForMathlib/Data/Set/Density.lean` — itself nontrivial
  machinery) plus real facts about the distribution of abundant/weird
  numbers. Scope is closer to this lane's #672/#9 "multi-session
  marathon" targets than to a one-cycle companion-result transport.
  **Do not pick this up expecting a quick win** — if attempted, it needs
  its own dedicated attack-plan entry in `OPEN_PROBLEMS.md`-adjacent
  tracking first, scoping the actual density-estimate mathematics before
  any Lean is written.
