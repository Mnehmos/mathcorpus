name = "MathCorpus"
defaultTargets = ["MathCorpus"]

# Pin mathlib to match `lean-toolchain` and the proofsearch verifier environment.
# Update both together when bumping the toolchain, and record the exact revision in each
# packet's `toolchain.mathlib_rev`.
[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4.git"
rev = "360da6fa66c1273b76b6b2d8c5666fd5ac2e3b56"

[[lean_lib]]
name = "MathCorpus"
